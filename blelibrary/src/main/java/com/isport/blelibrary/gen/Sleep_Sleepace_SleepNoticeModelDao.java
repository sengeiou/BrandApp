package com.isport.blelibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.isport.blelibrary.db.table.sleep.Sleep_Sleepace_SleepNoticeModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SLEEP__SLEEPACE__SLEEP_NOTICE_MODEL".
*/
public class Sleep_Sleepace_SleepNoticeModelDao extends AbstractDao<Sleep_Sleepace_SleepNoticeModel, Long> {

    public static final String TABLENAME = "SLEEP__SLEEPACE__SLEEP_NOTICE_MODEL";

    /**
     * Properties of entity Sleep_Sleepace_SleepNoticeModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property DeviceId = new Property(1, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property UserId = new Property(2, String.class, "userId", false, "USER_ID");
        public final static Property SleepNoticeTime = new Property(3, String.class, "sleepNoticeTime", false, "SLEEP_NOTICE_TIME");
        public final static Property Repeat = new Property(4, int.class, "repeat", false, "REPEAT");
        public final static Property IsOpen = new Property(5, boolean.class, "isOpen", false, "IS_OPEN");
    };


    public Sleep_Sleepace_SleepNoticeModelDao(DaoConfig config) {
        super(config);
    }
    
    public Sleep_Sleepace_SleepNoticeModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SLEEP__SLEEPACE__SLEEP_NOTICE_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"DEVICE_ID\" TEXT," + // 1: deviceId
                "\"USER_ID\" TEXT," + // 2: userId
                "\"SLEEP_NOTICE_TIME\" TEXT," + // 3: sleepNoticeTime
                "\"REPEAT\" INTEGER NOT NULL ," + // 4: repeat
                "\"IS_OPEN\" INTEGER NOT NULL );"); // 5: isOpen
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SLEEP__SLEEPACE__SLEEP_NOTICE_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Sleep_Sleepace_SleepNoticeModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(2, deviceId);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(3, userId);
        }
 
        String sleepNoticeTime = entity.getSleepNoticeTime();
        if (sleepNoticeTime != null) {
            stmt.bindString(4, sleepNoticeTime);
        }
        stmt.bindLong(5, entity.getRepeat());
        stmt.bindLong(6, entity.getIsOpen() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Sleep_Sleepace_SleepNoticeModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(2, deviceId);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(3, userId);
        }
 
        String sleepNoticeTime = entity.getSleepNoticeTime();
        if (sleepNoticeTime != null) {
            stmt.bindString(4, sleepNoticeTime);
        }
        stmt.bindLong(5, entity.getRepeat());
        stmt.bindLong(6, entity.getIsOpen() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Sleep_Sleepace_SleepNoticeModel readEntity(Cursor cursor, int offset) {
        Sleep_Sleepace_SleepNoticeModel entity = new Sleep_Sleepace_SleepNoticeModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // deviceId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // userId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // sleepNoticeTime
            cursor.getInt(offset + 4), // repeat
            cursor.getShort(offset + 5) != 0 // isOpen
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Sleep_Sleepace_SleepNoticeModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDeviceId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setUserId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSleepNoticeTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setRepeat(cursor.getInt(offset + 4));
        entity.setIsOpen(cursor.getShort(offset + 5) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Sleep_Sleepace_SleepNoticeModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Sleep_Sleepace_SleepNoticeModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
