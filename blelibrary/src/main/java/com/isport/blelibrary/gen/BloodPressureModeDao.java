package com.isport.blelibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.isport.blelibrary.db.table.w811w814.BloodPressureMode;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BLOOD_PRESSURE_MODE".
*/
public class BloodPressureModeDao extends AbstractDao<BloodPressureMode, Long> {

    public static final String TABLENAME = "BLOOD_PRESSURE_MODE";

    /**
     * Properties of entity BloodPressureMode.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property WristbandBloodPressureId = new Property(1, String.class, "wristbandBloodPressureId", false, "WRISTBAND_BLOOD_PRESSURE_ID");
        public final static Property UserId = new Property(2, String.class, "userId", false, "USER_ID");
        public final static Property DeviceId = new Property(3, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property Timestamp = new Property(4, long.class, "timestamp", false, "TIMESTAMP");
        public final static Property StrTimes = new Property(5, String.class, "strTimes", false, "STR_TIMES");
        public final static Property SystolicBloodPressure = new Property(6, int.class, "systolicBloodPressure", false, "SYSTOLIC_BLOOD_PRESSURE");
        public final static Property DiastolicBloodPressure = new Property(7, int.class, "diastolicBloodPressure", false, "DIASTOLIC_BLOOD_PRESSURE");
    };


    public BloodPressureModeDao(DaoConfig config) {
        super(config);
    }
    
    public BloodPressureModeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BLOOD_PRESSURE_MODE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"WRISTBAND_BLOOD_PRESSURE_ID\" TEXT," + // 1: wristbandBloodPressureId
                "\"USER_ID\" TEXT," + // 2: userId
                "\"DEVICE_ID\" TEXT," + // 3: deviceId
                "\"TIMESTAMP\" INTEGER NOT NULL ," + // 4: timestamp
                "\"STR_TIMES\" TEXT," + // 5: strTimes
                "\"SYSTOLIC_BLOOD_PRESSURE\" INTEGER NOT NULL ," + // 6: systolicBloodPressure
                "\"DIASTOLIC_BLOOD_PRESSURE\" INTEGER NOT NULL );"); // 7: diastolicBloodPressure
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BLOOD_PRESSURE_MODE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BloodPressureMode entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String wristbandBloodPressureId = entity.getWristbandBloodPressureId();
        if (wristbandBloodPressureId != null) {
            stmt.bindString(2, wristbandBloodPressureId);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(3, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(4, deviceId);
        }
        stmt.bindLong(5, entity.getTimestamp());
 
        String strTimes = entity.getStrTimes();
        if (strTimes != null) {
            stmt.bindString(6, strTimes);
        }
        stmt.bindLong(7, entity.getSystolicBloodPressure());
        stmt.bindLong(8, entity.getDiastolicBloodPressure());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BloodPressureMode entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String wristbandBloodPressureId = entity.getWristbandBloodPressureId();
        if (wristbandBloodPressureId != null) {
            stmt.bindString(2, wristbandBloodPressureId);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(3, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(4, deviceId);
        }
        stmt.bindLong(5, entity.getTimestamp());
 
        String strTimes = entity.getStrTimes();
        if (strTimes != null) {
            stmt.bindString(6, strTimes);
        }
        stmt.bindLong(7, entity.getSystolicBloodPressure());
        stmt.bindLong(8, entity.getDiastolicBloodPressure());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public BloodPressureMode readEntity(Cursor cursor, int offset) {
        BloodPressureMode entity = new BloodPressureMode( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // wristbandBloodPressureId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // userId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // deviceId
            cursor.getLong(offset + 4), // timestamp
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // strTimes
            cursor.getInt(offset + 6), // systolicBloodPressure
            cursor.getInt(offset + 7) // diastolicBloodPressure
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BloodPressureMode entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setWristbandBloodPressureId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setUserId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDeviceId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTimestamp(cursor.getLong(offset + 4));
        entity.setStrTimes(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setSystolicBloodPressure(cursor.getInt(offset + 6));
        entity.setDiastolicBloodPressure(cursor.getInt(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(BloodPressureMode entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(BloodPressureMode entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
