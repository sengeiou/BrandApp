package com.isport.blelibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.isport.blelibrary.db.table.watch_w516.Watch_W516_SettingModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WATCH__W516__SETTING_MODEL".
*/
public class Watch_W516_SettingModelDao extends AbstractDao<Watch_W516_SettingModel, Long> {

    public static final String TABLENAME = "WATCH__W516__SETTING_MODEL";

    /**
     * Properties of entity Watch_W516_SettingModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property DeviceId = new Property(2, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property Unit = new Property(3, boolean.class, "unit", false, "UNIT");
        public final static Property Language = new Property(4, boolean.class, "language", false, "LANGUAGE");
        public final static Property TimeFormat = new Property(5, boolean.class, "timeFormat", false, "TIME_FORMAT");
        public final static Property BrightScreen = new Property(6, boolean.class, "brightScreen", false, "BRIGHT_SCREEN");
        public final static Property HeartRateSwitch = new Property(7, boolean.class, "heartRateSwitch", false, "HEART_RATE_SWITCH");
    };


    public Watch_W516_SettingModelDao(DaoConfig config) {
        super(config);
    }
    
    public Watch_W516_SettingModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WATCH__W516__SETTING_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"USER_ID\" TEXT," + // 1: userId
                "\"DEVICE_ID\" TEXT," + // 2: deviceId
                "\"UNIT\" INTEGER NOT NULL ," + // 3: unit
                "\"LANGUAGE\" INTEGER NOT NULL ," + // 4: language
                "\"TIME_FORMAT\" INTEGER NOT NULL ," + // 5: timeFormat
                "\"BRIGHT_SCREEN\" INTEGER NOT NULL ," + // 6: brightScreen
                "\"HEART_RATE_SWITCH\" INTEGER NOT NULL );"); // 7: heartRateSwitch
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WATCH__W516__SETTING_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Watch_W516_SettingModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(3, deviceId);
        }
        stmt.bindLong(4, entity.getUnit() ? 1L: 0L);
        stmt.bindLong(5, entity.getLanguage() ? 1L: 0L);
        stmt.bindLong(6, entity.getTimeFormat() ? 1L: 0L);
        stmt.bindLong(7, entity.getBrightScreen() ? 1L: 0L);
        stmt.bindLong(8, entity.getHeartRateSwitch() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Watch_W516_SettingModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(3, deviceId);
        }
        stmt.bindLong(4, entity.getUnit() ? 1L: 0L);
        stmt.bindLong(5, entity.getLanguage() ? 1L: 0L);
        stmt.bindLong(6, entity.getTimeFormat() ? 1L: 0L);
        stmt.bindLong(7, entity.getBrightScreen() ? 1L: 0L);
        stmt.bindLong(8, entity.getHeartRateSwitch() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Watch_W516_SettingModel readEntity(Cursor cursor, int offset) {
        Watch_W516_SettingModel entity = new Watch_W516_SettingModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // userId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // deviceId
            cursor.getShort(offset + 3) != 0, // unit
            cursor.getShort(offset + 4) != 0, // language
            cursor.getShort(offset + 5) != 0, // timeFormat
            cursor.getShort(offset + 6) != 0, // brightScreen
            cursor.getShort(offset + 7) != 0 // heartRateSwitch
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Watch_W516_SettingModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDeviceId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setUnit(cursor.getShort(offset + 3) != 0);
        entity.setLanguage(cursor.getShort(offset + 4) != 0);
        entity.setTimeFormat(cursor.getShort(offset + 5) != 0);
        entity.setBrightScreen(cursor.getShort(offset + 6) != 0);
        entity.setHeartRateSwitch(cursor.getShort(offset + 7) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Watch_W516_SettingModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Watch_W516_SettingModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
