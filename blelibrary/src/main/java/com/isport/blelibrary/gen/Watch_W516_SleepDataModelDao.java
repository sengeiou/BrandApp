package com.isport.blelibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.isport.blelibrary.db.table.watch_w516.Watch_W516_SleepDataModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WATCH__W516__SLEEP_DATA_MODEL".
*/
public class Watch_W516_SleepDataModelDao extends AbstractDao<Watch_W516_SleepDataModel, Long> {

    public static final String TABLENAME = "WATCH__W516__SLEEP_DATA_MODEL";

    /**
     * Properties of entity Watch_W516_SleepDataModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property DeviceId = new Property(2, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property Timestamp = new Property(3, long.class, "timestamp", false, "TIMESTAMP");
        public final static Property DateStr = new Property(4, String.class, "dateStr", false, "DATE_STR");
        public final static Property TotalSleep = new Property(5, int.class, "totalSleep", false, "TOTAL_SLEEP");
        public final static Property TotalDeepSleep = new Property(6, int.class, "totalDeepSleep", false, "TOTAL_DEEP_SLEEP");
        public final static Property TotalAwake = new Property(7, int.class, "totalAwake", false, "TOTAL_AWAKE");
        public final static Property TotalLightSleep = new Property(8, int.class, "totalLightSleep", false, "TOTAL_LIGHT_SLEEP");
    };


    public Watch_W516_SleepDataModelDao(DaoConfig config) {
        super(config);
    }
    
    public Watch_W516_SleepDataModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WATCH__W516__SLEEP_DATA_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"USER_ID\" TEXT," + // 1: userId
                "\"DEVICE_ID\" TEXT," + // 2: deviceId
                "\"TIMESTAMP\" INTEGER NOT NULL ," + // 3: timestamp
                "\"DATE_STR\" TEXT," + // 4: dateStr
                "\"TOTAL_SLEEP\" INTEGER NOT NULL ," + // 5: totalSleep
                "\"TOTAL_DEEP_SLEEP\" INTEGER NOT NULL ," + // 6: totalDeepSleep
                "\"TOTAL_AWAKE\" INTEGER NOT NULL ," + // 7: totalAwake
                "\"TOTAL_LIGHT_SLEEP\" INTEGER NOT NULL );"); // 8: totalLightSleep
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WATCH__W516__SLEEP_DATA_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Watch_W516_SleepDataModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(3, deviceId);
        }
        stmt.bindLong(4, entity.getTimestamp());
 
        String dateStr = entity.getDateStr();
        if (dateStr != null) {
            stmt.bindString(5, dateStr);
        }
        stmt.bindLong(6, entity.getTotalSleep());
        stmt.bindLong(7, entity.getTotalDeepSleep());
        stmt.bindLong(8, entity.getTotalAwake());
        stmt.bindLong(9, entity.getTotalLightSleep());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Watch_W516_SleepDataModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(3, deviceId);
        }
        stmt.bindLong(4, entity.getTimestamp());
 
        String dateStr = entity.getDateStr();
        if (dateStr != null) {
            stmt.bindString(5, dateStr);
        }
        stmt.bindLong(6, entity.getTotalSleep());
        stmt.bindLong(7, entity.getTotalDeepSleep());
        stmt.bindLong(8, entity.getTotalAwake());
        stmt.bindLong(9, entity.getTotalLightSleep());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Watch_W516_SleepDataModel readEntity(Cursor cursor, int offset) {
        Watch_W516_SleepDataModel entity = new Watch_W516_SleepDataModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // userId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // deviceId
            cursor.getLong(offset + 3), // timestamp
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // dateStr
            cursor.getInt(offset + 5), // totalSleep
            cursor.getInt(offset + 6), // totalDeepSleep
            cursor.getInt(offset + 7), // totalAwake
            cursor.getInt(offset + 8) // totalLightSleep
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Watch_W516_SleepDataModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDeviceId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTimestamp(cursor.getLong(offset + 3));
        entity.setDateStr(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTotalSleep(cursor.getInt(offset + 5));
        entity.setTotalDeepSleep(cursor.getInt(offset + 6));
        entity.setTotalAwake(cursor.getInt(offset + 7));
        entity.setTotalLightSleep(cursor.getInt(offset + 8));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Watch_W516_SleepDataModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Watch_W516_SleepDataModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
