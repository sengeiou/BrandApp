package com.isport.blelibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.isport.blelibrary.db.table.w526.Device_TempTable;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DEVICE__TEMP_TABLE".
*/
public class Device_TempTableDao extends AbstractDao<Device_TempTable, Long> {

    public static final String TABLENAME = "DEVICE__TEMP_TABLE";

    /**
     * Properties of entity Device_TempTable.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property WristbandTemperatureId = new Property(2, String.class, "wristbandTemperatureId", false, "WRISTBAND_TEMPERATURE_ID");
        public final static Property DeviceId = new Property(3, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property Centigrade = new Property(4, String.class, "centigrade", false, "CENTIGRADE");
        public final static Property Fahrenheit = new Property(5, String.class, "fahrenheit", false, "FAHRENHEIT");
        public final static Property StrDate = new Property(6, String.class, "strDate", false, "STR_DATE");
        public final static Property Timestamp = new Property(7, long.class, "timestamp", false, "TIMESTAMP");
    };


    public Device_TempTableDao(DaoConfig config) {
        super(config);
    }
    
    public Device_TempTableDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DEVICE__TEMP_TABLE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"USER_ID\" TEXT," + // 1: userId
                "\"WRISTBAND_TEMPERATURE_ID\" TEXT," + // 2: wristbandTemperatureId
                "\"DEVICE_ID\" TEXT," + // 3: deviceId
                "\"CENTIGRADE\" TEXT," + // 4: centigrade
                "\"FAHRENHEIT\" TEXT," + // 5: fahrenheit
                "\"STR_DATE\" TEXT," + // 6: strDate
                "\"TIMESTAMP\" INTEGER NOT NULL );"); // 7: timestamp
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DEVICE__TEMP_TABLE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Device_TempTable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String wristbandTemperatureId = entity.getWristbandTemperatureId();
        if (wristbandTemperatureId != null) {
            stmt.bindString(3, wristbandTemperatureId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(4, deviceId);
        }
 
        String centigrade = entity.getCentigrade();
        if (centigrade != null) {
            stmt.bindString(5, centigrade);
        }
 
        String fahrenheit = entity.getFahrenheit();
        if (fahrenheit != null) {
            stmt.bindString(6, fahrenheit);
        }
 
        String strDate = entity.getStrDate();
        if (strDate != null) {
            stmt.bindString(7, strDate);
        }
        stmt.bindLong(8, entity.getTimestamp());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Device_TempTable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String wristbandTemperatureId = entity.getWristbandTemperatureId();
        if (wristbandTemperatureId != null) {
            stmt.bindString(3, wristbandTemperatureId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(4, deviceId);
        }
 
        String centigrade = entity.getCentigrade();
        if (centigrade != null) {
            stmt.bindString(5, centigrade);
        }
 
        String fahrenheit = entity.getFahrenheit();
        if (fahrenheit != null) {
            stmt.bindString(6, fahrenheit);
        }
 
        String strDate = entity.getStrDate();
        if (strDate != null) {
            stmt.bindString(7, strDate);
        }
        stmt.bindLong(8, entity.getTimestamp());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Device_TempTable readEntity(Cursor cursor, int offset) {
        Device_TempTable entity = new Device_TempTable( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // userId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // wristbandTemperatureId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // deviceId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // centigrade
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // fahrenheit
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // strDate
            cursor.getLong(offset + 7) // timestamp
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Device_TempTable entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setWristbandTemperatureId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDeviceId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setCentigrade(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setFahrenheit(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setStrDate(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setTimestamp(cursor.getLong(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Device_TempTable entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Device_TempTable entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
