package brandapp.isport.com.basicres.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import brandapp.isport.com.basicres.entry.WatchTargetBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WATCH_TARGET_BEAN".
*/
public class WatchTargetBeanDao extends AbstractDao<WatchTargetBean, Long> {

    public static final String TABLENAME = "WATCH_TARGET_BEAN";

    /**
     * Properties of entity WatchTargetBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property DeviceId = new Property(2, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property TargetStep = new Property(3, int.class, "targetStep", false, "TARGET_STEP");
        public final static Property TargetDistance = new Property(4, int.class, "targetDistance", false, "TARGET_DISTANCE");
        public final static Property TargetCalorie = new Property(5, int.class, "targetCalorie", false, "TARGET_CALORIE");
    };


    public WatchTargetBeanDao(DaoConfig config) {
        super(config);
    }
    
    public WatchTargetBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WATCH_TARGET_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"USER_ID\" TEXT," + // 1: userId
                "\"DEVICE_ID\" TEXT," + // 2: deviceId
                "\"TARGET_STEP\" INTEGER NOT NULL ," + // 3: targetStep
                "\"TARGET_DISTANCE\" INTEGER NOT NULL ," + // 4: targetDistance
                "\"TARGET_CALORIE\" INTEGER NOT NULL );"); // 5: targetCalorie
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WATCH_TARGET_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, WatchTargetBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(3, deviceId);
        }
        stmt.bindLong(4, entity.getTargetStep());
        stmt.bindLong(5, entity.getTargetDistance());
        stmt.bindLong(6, entity.getTargetCalorie());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, WatchTargetBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(3, deviceId);
        }
        stmt.bindLong(4, entity.getTargetStep());
        stmt.bindLong(5, entity.getTargetDistance());
        stmt.bindLong(6, entity.getTargetCalorie());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public WatchTargetBean readEntity(Cursor cursor, int offset) {
        WatchTargetBean entity = new WatchTargetBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // userId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // deviceId
            cursor.getInt(offset + 3), // targetStep
            cursor.getInt(offset + 4), // targetDistance
            cursor.getInt(offset + 5) // targetCalorie
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, WatchTargetBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDeviceId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTargetStep(cursor.getInt(offset + 3));
        entity.setTargetDistance(cursor.getInt(offset + 4));
        entity.setTargetCalorie(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(WatchTargetBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(WatchTargetBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
